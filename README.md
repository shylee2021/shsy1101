# Wedding Invitation

## 실행

Node.js 24 LTS와 pnpm이 필요합니다.

```bash
pnpm install
pnpm dev
```

청첩장 정보는 `src/config.ts`에서 관리합니다.

## 빌드 확인

```bash
pnpm build
pnpm preview
```

## 배포

`main` 브랜치에 푸시하면 GitHub Pages에 자동 배포됩니다.
